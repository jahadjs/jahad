{
  "name": "@mr0bread/viole-core",
  "version": "0.0.1",
  "license": "MIT",
  "main": "dist/main.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "ts:build": "yarn tsc --p tsconfig.build.json",
    "ts:types": "yarn tsc --p tsconfig.types.json",
    "build": "yarn ts:build && yarn ts:types"
  },
  "dependencies": {
    "chalk": "5.1.2",
    "fastify": "4.8.1"
  },
  "devDependencies": {
    "@types/node": "18.11.0",
    "typescript": "4.8.4",
    "vite-node": "0.24.3",
    "vitest": "0.24.3"
  }
}
